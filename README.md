# Mini Dashboard (Streamlit)

דוגמה קטנה לדשבורד עם נתוני דמה: פילטרים, מדדים, גרפים וטבלה.

## התקנה

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```

## הרצה

```bash
streamlit run DASHBOARD.py
```

אם השרת עולה, הדשבורד ייפתח בדפדפן אוטומטית.
